# CHANGELOG

確認できたGit履歴、既存文書、今回の作業記録のみを記載する。Git履歴以前の変更は推測しない。

## 2026-09-20

### 追加

- トップの常設フッター、ゲーム一覧、各作品詳細、ミニゲーム一覧へ、画像・文章・ゲームデータの無断転載・無断配布を禁止する注意書きを追加。
- スマホ幅で常設フッターと下部メニューが重ならないよう、メニュー下端の余白を調整。
- 銀河中央へログイン・コイン不要の無料試験ガチャを追加。10連演出と端末内保存の81枚コレクション図鑑を実装。
- Battle a la carte 40種を、素材C、料理SR、イベントSSR、キャラクターURとして登録。
- 架空運輸41種を登録。緑の車両5種と架空社員30名の画像を生成し、既存社員6枠と合わせて役職別レアリティを設定。

### 変更

- 中央GACHAアプリを回転ゲームケースより前面へ配置し、クリック競合を防止。
- ガチャ画面をPC、スマホ縦、短い横画面へレスポンシブ対応。横画面では既存モーダル上余白を縮小。

### 修正

- 公開版で既存架空運輸社員カードの画像が欠けないよう、参照する6画像を `images/gacha/` へ同梱。

### テスト

- ポータル構造テストとPC・スマホ相当幅の表示確認を実施。
- `trial-gacha.test.cjs` を追加し、カード構成、確率境界、10連、保存値を検証。
- 全81枚の参照画像が公開用作業ツリー内に存在することを自動検証。
- PC、360×800、430×932、844×390で中央アプリ、10連結果、81枚図鑑、閉じる導線、横はみ出しなしを確認。実機は未確認。

## 2026-09-16

### 変更

- 最新実装に合わせ、標準5文書の作品数、選択順、Unity配布方式、共通操作デッキ、現在状態を更新。
- 短い画面に残っていた38pxパッド指定を削除。回転ボタンの最小高さは通常62px、短い横画面では52pxへ統一。

### テスト

- リリース前4テスト、変更JS構文検査、Git差分の空白検査が通過。
- 最終CSS調整後、テトリスを320×568、360×800、412×915、568×320、1366×768で再確認。操作44px以上、盤面との重なり・画面外・スクロールなし。
- 小型画面のUnity紹介モーダルに横はみ出しなし。作品順、ゲーム一覧とホームへの復帰、公開Windows ZIPの存在とハッシュ一致を確認。実機は未確認。

### Git記録

- `4385ee4` Add standalone Unity 3D game and responsive arcade controls。既存ポータルリポジトリの `main` へpush済み。標準5文書を同時反映し、未公開共通アカウントUIと認証情報は含めない。

## 2026-09-15

### 追加

- Unity版（3D版）をWeb版とは別作品として、2番目の軌道カード・ゲーム一覧・専用詳細へ追加。紹介には実際の3D対戦画面を使用。
- 専用リポジトリ `donadonaa24-cyber/battle-a-la-carte-3d` を作成。Pagesで紹介、Release `v0.1.0` でWindows 64-bit ZIPを公開。
- 配布ZIPは既存Windows成果物195ファイル、269,210,425 bytes。デバッグバックアップ・PDB/MDB・ログを除外し、構造・SHA-256を検証。Unity本体の再ビルドは実施していない。

### 変更

- 選択順をWeb版Battle a la carte、Unity版、架空運輸、推し駒battle、混ぜるな危険へ変更。選択総数を動的表示へ変更。
- 共通操作デッキへ既存の開始・リセット・神経衰弱入力DOMを移設。左下移動、右下テトリス回転・1段下降へ配置。
- 縦向きは下部、横向き/PCは左右の操作域を確保。Safe Areaと動的ビューポート対応を追加。
- タッチのテトリス上回転とブロック崩し上下ボタンを非表示。キーボードWASD/矢印入力は維持。

### 修正

- 方向操作不要ゲームでパッド領域が残り盤面を縮小するCSS優先順位を修正。
- 神経衰弱の回転表示時のSafe Areaと終了ボタン余白を調整し、全30枚を見やすく配置。
- ミニゲーム中はポータルヘッダーを隠し、盤面への重なりを防止。

### 設定変更

- Unity版Pagesは `main` / ルート。HTTPS既定設定を維持。有料プラン・サービスへの変更なし。
- 別配布作業ツリーとローカルZIPはポータルのGit管理対象外。
- 未公開の共通アカウントUI・認証設定は今回の公開に含めない。

### テスト

- `common.test.cjs`、`arcade.test.cjs`、`arcade-layout.test.cjs`、`portal.test.cjs` と変更JS構文検査が通過。
- 既存ボタンDOMを複製せず移設すること、選択順、Unity ZIP導線、exe直接起動禁止の回帰検証を追加。
- 開始済み8ゲームを8サイズのブラウザ寸法で検証し全64通り通過。操作44px以上、盤面重なり・画面外・スクロールなし。スマホ実機は未確認。

### Git記録

- `343f078` Add standalone Unity 3D Windows download website（Unity版専用リポジトリ）

## 2026-09-14

### 追加

- `AGENTS.md` と `docs/` 配下のプロジェクト仕様、現在状態、変更履歴、技術構成を追加。
- 秘密情報ファイルとローカル作業用 `docs/ACTIVE_TASKS.md` を除外する `.gitignore` を追加。

### 変更

- 新しいCodexチャットでは `AGENTS.md` と `docs/CURRENT_STATE.md` を先に読み、必要な文書とソースだけを確認する運用へ統一。
- 標準の5文書をソースコードと同じGitHubリポジトリで管理するルールを追加。
- APIキー、トークン、パスワード、Secret、PC固有情報・絶対パスを文書やGitへ含めない規則を明文化。
- 作業終了時は、実際に影響を受けた文書だけを更新する運用へ整理。

### テスト

- ドキュメントのみの変更のため、フル回帰テストは再実行していない。
- コード、既存README、Supabase migration、テスト、公開用Git履歴との整合を確認。

### Git記録

- `44c102f` Add project documentation management rules

## 2026-09-12

### 追加

- Supabase `aniani-common` に共通プロフィール、ウォレット、コイン台帳、カード、所持カード、デイリー、ガチャ、実績のDB構造を追加。
- ポータルのローカル作業版へ、プレイヤー登録・ログイン・パスワード再設定、共通コイン、デイリー、1回/10回ガチャ、16枚のコレクション図鑑UIを追加。
- `common/api.js`、`common/config.js`、`common/portal.js`、`common/portal.css`、`common.test.cjs`、Supabase migration・検証SQL・READMEを追加。

### 修正

- Supabaseの公開可能キーに欠けていたハイフンを修正し、公開カードAPIへの接続を復旧。
- ガチャのリクエストIDを送信前に端末へ保存し、応答不明時の二重消費を防止。

### 設定変更

- SupabaseのSite URLとリダイレクト許可先をGitHub Pagesの `index.html` に設定。
- Supabase Freeプランを維持。カスタムSMTPは未設定。

### テスト

- RLS、他人データ遮断、直接書込み禁止、デイリー・ガチャの冪等性、残高不足、10連、匿名制限をロールバック付きSQLで確認。
- 共通API、ポータル、ミニゲーム、画面復帰テストを実行し通過。
- ブラウザで公開カード16枚、排出率、匿名ガチャ無効、カード条件、モーダル終了を確認。

## 2026-09-11

### 変更

- 公開済み作品の状態を「公開中＆追加要素可能性あり」に統一。
- Battle a la carteと架空運輸の導線を「ホームページへ」「ウェブ版で遊ぶ」「スマホ版で遊ぶ」に統一。
- ポータル内の積み込みゲーム紹介名を「架空運輸」に変更し、ホームリンクへ `?view=home` を指定。
- 仮想パッドを右下へ移動。テトリスの回転操作とNEXT 2件表示、宇宙背景を調整。
- 落ちものパズルを、解ける配置を生成する8パズルへ置換。

### 修正

- ミニゲーム中の×で一覧へ戻る処理、明示的なホーム復帰ボタン、Fullscreen/cleanup失敗時の復帰を修正。

### テスト

- ポータルリンクテスト、ミニゲームロジックテスト、画面復帰テストを追加・更新。

### Git記録

- `39f7416` Unify game introductions and fix logistics homepage navigation
- `4ff9023` Improve arcade controls and cosmic Tetris; replace falling puzzle with 8 puzzle
- `8204170` Fix arcade exit controls and add explicit return-home action

## 2026-09-09

### 追加

- もぐら、うさぎ、犬、カウボーイの生成画像と生成メモを追加。
- ミニゲームの全画面処理、ビューポートフィット、神経衰弱の横画面フォールバックを追加。
- ミニゲーム自動テストを追加。

### 変更

- 〇×ゲームを9×9・5目並べへ変更。
- テトリスへ次の2ピース表示を追加。
- ミニゲームを全画面固定UIへ調整。

### 修正

- ブロック崩し等で不足していた料理画像素材を復元。

### Git記録

- `084e564` Fix fullscreen mini games, add arcade art and restore missing recipe assets

## 2026-09-07

### 追加

- 独立リポジトリとしてポータル一式をGitへ初回登録。
- `index.html`、ポータル/銀河/アーケード用CSS・JS、README、Google確認HTMLを追加。

### Git記録

- `89490d8` Add files via upload

## 日付不明

### 追加・変更

- Git初回登録以前の制作経緯は確認できないため未記載。
