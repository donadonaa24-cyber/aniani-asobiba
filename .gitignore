# Local coordination file for parallel Codex tasks
docs/ACTIVE_TASKS.md

# Local credentials and environment overrides
supabase.txt
.env
.env.*
!.env.example

# Separate distribution repository and local release artifacts
/.publish-battle-3d/
/release-artifacts/
